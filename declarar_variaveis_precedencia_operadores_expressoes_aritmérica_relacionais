#include <stdio.h>
#include <stdlib.h>

int main () {

  /* declaracoes */
  
  int num1, num2, soma;
  
  /* programa */
  
  printf ("Entre com dois numeros inteiros: ");
  scanf ("%d %d, &num1, &num2);
  
  printf ("Os numeros lidos foram %d e %d \n", num1, num2);
  
  soma = num 1 + num 2;
  
  printf ("O resultado da soma %d com %d eh igual %d \n", num1, num2, soma);
  
  /* fim do programa */
  
  return 0;
}
